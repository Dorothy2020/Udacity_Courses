# A string list method  that takes in arguments consisting of a list elements joined by a separator string
# use of "\n".join that does not print out the elements in a square bracket
# "\n" as the separator so that there is a newline between each element
new_str = "\n".join(["fore","aft","starboard","port"])
print(new_str)

